import React from 'react';

//Simple wrapper that takes children as a prop and renders them inside
// an element with a class of container
const Container = ({ children }) => {

return

{children}

;

};

export default Container;

